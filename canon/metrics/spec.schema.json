{
  "contract_id": "HP_METRIC_SPEC_V1",
  "properties": {
    "metric_id": { "type": "string" },
    "derivation": {
      "requires_signals": ["signal_id"],
      "requires_metrics": ["metric_id"],
      "fallback": "metric_id"
    },
    "logic_method": { "enum": ["markov_chain", "logistic_regression", "sum_aggregate", "threshold_gate"] },
    "polarity": { "enum": ["higher_is_better", "lower_is_better", "neutral"] },
    "citations": {
      "supporting_definition": ["ref_id"],
      "validation_method": ["ref_id"]
    },
    "scopes": ["pre_match", "post_match", "individual_nas", "squad_engineering"]
  }
}
